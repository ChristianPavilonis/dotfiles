themes {
    omarchy {
        fg "{{ foreground }}"
        bg "{{ background }}"
        black "{{ background }}"
        red "{{ red }}"
        green "{{ green }}"
        yellow "{{ yellow }}"
        blue "{{ blue }}"
        magenta "{{ magenta }}"
        cyan "{{ cyan }}"
        white "{{ bright_foreground }}"
        orange "{{ orange }}"
    }
}
