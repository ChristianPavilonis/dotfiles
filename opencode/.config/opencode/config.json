{
  "$schema": "https://opencode.ai/config.json",
  "theme": "kanagawa",
  "autoshare": false,
  "autoupdate": true,
  "layout": "stretch",
   "plugin": [
    "opencode-anthropic-auth@latest"
  ],
  "command": {
    "pr-dashboard": {
      "template": "Run the PR dashboard script: `bun run ~/.config/opencode/scripts/pr-dashboard.ts`. Report what PRs were found, what was written to the daily note, and any agent sessions spawned. If the user passes arguments, append them to the command (e.g. `--no-agents` or `--dry-run`).",
      "description": "Fetch PRs to review and PRs needing changes, update daily note"
    },
    "issue-resolver": {
      "template": "Run the issue resolver script: `bun run ~/.config/opencode/scripts/issue-resolver.ts`. Report what issues were found, worktrees created, and agent sessions spawned. If the user passes arguments, append them to the command (e.g. `--no-agents` or `--dry-run`).",
      "description": "Auto-resolve GitHub issues labeled 'agent' with AI"
    }
  },
  "keybinds": {
    "agent_cycle": "tab",
    "command_list": "ctrl+/",
    "variant_cycle": "ctrl+v",
    "input_paste": "none"
  },
  "permission": {
    "external_directory": {
      "~/.cargo/**": "allow",
      "~/Documents/MyObsidianVault/**": "allow",
      "~/worktrees/**": "allow"
    }
  },
  "mcp": {
    "chrome-devtools": {
      "type": "local",
      "command": ["npx", "chrome-devtools-mcp@latest"],
      "enabled": true
    },
    "gitbutler" : {
       "type": "local",
      "command": ["but", "mcp"],
      "enabled": false,
    }
  }
}
